dataset:
  name: wildtrack

detection:
  name: mvdetr

tracking:
  name: deep_sort_bev
  params:
    max_age: 4
    dist_thresh: 0.4
    gating_dist: 40.0
    use_visibility: true
    visibility_weight: 0.6
    vis_score_thresh: 0.8

